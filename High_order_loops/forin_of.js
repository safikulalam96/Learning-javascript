const array=[2,5,56,45,3,6,7,9]

for (const key of array) {
     console.log(key)   //we cannot use dot operator.
}

const greeting="hello world"

for (const key of greeting) {
    console.log(`Each value of greeting is ${key}`)
}